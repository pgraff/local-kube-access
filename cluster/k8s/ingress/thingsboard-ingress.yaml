apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: thingsboard-ingress
  namespace: iot
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: thingsboard.tailc2013b.ts.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: thingsboard
            port:
              number: 9090

